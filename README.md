## Numbers Checker application

Open terminal do following steps

1. Install project:

    ```shell
    composer install
    ```

 2. application:
    ```shell
    php app/app.php
    ```

3. (Optional) Run tests with:

    ```shell
    bin/phpunit
    ```